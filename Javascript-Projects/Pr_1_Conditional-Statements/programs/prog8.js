// Compare two numbers

let num1 = 6, num2 = 6;

if (num1 < num2) {
    console.log(`${num1} is smaller than ${num2}.`);
} else if (num1 > num2) {
    console.log(`${num2} is smaller than ${num1}.`);
} else {
    console.log(`Both ${num1} and ${num2} numbers are equal.`);
}